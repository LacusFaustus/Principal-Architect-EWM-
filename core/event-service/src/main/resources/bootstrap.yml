spring:
  application:
    name: event-service
  cloud:
    config:
      enabled: true
      uri: http://config-server:8082
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
        multiplier: 1.5
        max-interval: 5000